<section id="about" style="padding-top:20px">
  <div class="wrap">
    <div class="about reveal">
      <div>
        <div class="about-name">Softmelody</div>
        <div class="about-role">AI CONSULTANCY &amp; PRODUCT STUDIO</div>
        <div class="about-loc">
          <svg
            width="14"
            height="14"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2"
            stroke-linecap="round"
            stroke-linejoin="round"
          >
            <path d="M20 10c0 6-8 12-8 12S4 16 4 10a8 8 0 1 1 16 0z"></path>
            <circle cx="12" cy="10" r="3"></circle>
          </svg>
          Remote-first, worldwide
        </div>
      </div>
      <p class="about-bio">
        An independent consultancy building production AI for <strong
          >fintech, medtech, and communications</strong
        >. Every product on this page — Melody, Authia, OpenPOS, Flow, XDB — is designed, built,
        and maintained under Softmelody, and the same engineering standard goes into
        <strong>your</strong> engagement.
      </p>
    </div>
  </div>
</section>

<style>
  .about {
    border: 1px solid var(--line);
    border-radius: 12px;
    padding: 56px;
    background: var(--bg-2);
    display: grid;
    grid-template-columns: 1fr 1.6fr;
    gap: 48px;
    align-items: center;
  }
  .about-name {
    font-family: var(--font-body);
    font-size: 30px;
    font-weight: 600;
    letter-spacing: -0.02em;
  }
  .about-role {
    font-family: var(--font-mono);
    font-size: 13px;
    color: var(--accent);
    margin-top: 8px;
    letter-spacing: 0.06em;
  }
  .about-loc {
    font-size: 13px;
    color: var(--faint);
    margin-top: 18px;
    display: flex;
    align-items: center;
    gap: 8px;
  }
  .about-bio {
    color: var(--muted);
    font-size: 16px;
    line-height: 1.75;
  }
  .about-bio strong {
    color: var(--text);
    font-weight: 500;
  }
  @media (max-width: 800px) {
    .about {
      grid-template-columns: 1fr;
      padding: 36px 28px;
    }
  }
</style>
